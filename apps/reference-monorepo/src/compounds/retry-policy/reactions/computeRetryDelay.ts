// Auto-scaffolded reaction (use case workflow).
import type { RetrySpec, BackoffComputer } from "../public.js";
export async function computeRetryDelay(input: unknown): Promise<unknown> {
  void input;
  void {} as RetrySpec | undefined;
  void {} as BackoffComputer | undefined;
  return { ok: true, reaction: "computeRetryDelay" };
}
