// Auto-scaffolded adapter.
import type { UserRepository, User, UserId } from "../public.js";
export class PostgresUserRepository implements UserRepository {
  describe(): string {
    return "PostgresUserRepository";
  }
}
