// Auto-scaffolded reaction (use case workflow).
import type { Subscription, PriceId, BillingRepository } from "../public.js";
export async function createSubscription(input: unknown): Promise<unknown> {
  void input;
  return { ok: true, reaction: "createSubscription" };
}
