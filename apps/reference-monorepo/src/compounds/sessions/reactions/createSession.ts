// Auto-scaffolded reaction (use case workflow).
import type { Session, SessionStore } from "../public.js";
export async function createSession(input: unknown): Promise<unknown> {
  void input;
  return { ok: true, reaction: "createSession" };
}
