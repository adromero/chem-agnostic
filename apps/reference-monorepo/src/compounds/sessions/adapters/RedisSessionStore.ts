// Auto-scaffolded adapter.
import type { SessionStore, Session, SessionId } from "../public.js";
export class RedisSessionStore implements SessionStore {
  describe(): string {
    return "RedisSessionStore";
  }
}
