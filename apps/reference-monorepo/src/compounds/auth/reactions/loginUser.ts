// Auto-scaffolded reaction (use case workflow).
import type { Credential, AuthGateway } from "../public.js";
export async function loginUser(input: unknown): Promise<unknown> {
  void input;
  return { ok: true, reaction: "loginUser" };
}
