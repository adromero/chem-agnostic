// Auto-scaffolded reaction (use case workflow).
import type { Token, AuthGateway } from "../public.js";
export async function logoutUser(input: unknown): Promise<unknown> {
  void input;
  void {} as Token | undefined;
  void {} as AuthGateway | undefined;
  return { ok: true, reaction: "logoutUser" };
}
