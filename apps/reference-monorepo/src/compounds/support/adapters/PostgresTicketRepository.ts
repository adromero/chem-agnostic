// Auto-scaffolded adapter.
import type { TicketRepository, SupportTicket, TicketId } from "../public.js";
export class PostgresTicketRepository implements TicketRepository {
  describe(): string {
    return "PostgresTicketRepository";
  }
}
